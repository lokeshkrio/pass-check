import string

LOWERCASE = set(string.ascii_lowercase)
UPPERCASE = set(string.ascii_uppercase)
DIGITS = set(string.digits)
SYMBOLS = set(string.punctuation)
WHITESPACE = set(string.whitespace)

UNICODE_ESTIMATED_POOL = 100_000
